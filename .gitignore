# Diretório de dependências de módulos
/node_modules

# Arquivos de log e debug
npm-debug.log
yarn-debug.log
yarn-error.log
/logs
*.log
*.err

# Arquivos de ambiente sensíveis (segredos/configurações)
# NUNCA envie estes para o Git. Use um arquivo de exemplo (ex: .env.example)
.env
.env.*
.vscode
.local

# Resultados de build/distribuição
# Ajuste o nome da pasta conforme o seu bundler/framework (ex: dist, build, public, out)
/dist
/build
/out
/coverage
/tmp

# Arquivos de cache e temporários
.npm
.eslintcache
.next/         # Para projetos Next.js
.nuxt/        # Para projetos Nuxt
/web-ext-artifacts/

# Arquivos de sistema e editor
.DS_Store
Thumbs.db

# Ignorar arquivos gerados pelo Yarn Berry (opcional, se estiver usando)
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*